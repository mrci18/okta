version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8
  pre_build:
    commands:
      - pwd

  build:
    commands:
      - echo "Deploy CFT to Create User and set IdP 1"
      # Set VPCID env variable
  post_build:
    commands:
      - echo Done!
